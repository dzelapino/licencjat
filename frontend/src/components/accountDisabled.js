import styles from "@/styles/components/Layout.module.scss";

const AccountDisabled = () => {

  return (
    <div className={styles.AccountDisabled}>
      Your account has been banned
    </div>
  );
};

export default AccountDisabled;
